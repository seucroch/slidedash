<h1>All Albums</h1>
<% @albums.each do |album| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to album.title, album %>
      </h4>
    </div>
  </div>
<% end %>